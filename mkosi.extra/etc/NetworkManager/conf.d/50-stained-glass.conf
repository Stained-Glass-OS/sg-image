# Stained Glass OS: NetworkManager hands DNS to systemd-resolved, which the
# domain roles configure (sg-dc-provision, sg-domain-join write
# resolved.conf.d). Every wired adapter gets a DHCP profile by default.
[main]
dns=systemd-resolved
rc-manager=unmanaged
