# Mirrors mkosi's own default ESP definition. Present only because defining
# mkosi.repart/ replaces the defaults wholesale -- if this file is missing,
# the image gets no ESP and does not boot.
[Partition]
Type=esp
Format=vfat
CopyFiles=/boot:/
CopyFiles=/efi:/
SizeMinBytes=512M
SizeMaxBytes=512M
